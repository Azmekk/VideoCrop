<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="VideoCrop.App.Views.CropDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctrl="using:VideoCrop.App.Controls"
    Title="Edit crop"
    PrimaryButtonText="Apply"
    SecondaryButtonText="Reset"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    mc:Ignorable="d">

    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMaxWidth">1400</x:Double>
        <x:Double x:Key="ContentDialogMaxHeight">800</x:Double>
        <x:Double x:Key="ContentDialogMinWidth">400</x:Double>
        <x:Double x:Key="ContentDialogMinHeight">400</x:Double>
    </ContentDialog.Resources>

    <Grid x:Name="RootGrid" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="6">
            <TextBlock Text="Aspect:" VerticalAlignment="Center" />
            <ComboBox x:Name="AspectCombo" SelectionChanged="OnAspectChanged">
                <ComboBoxItem Content="Free" Tag="Free" />
                <ComboBoxItem Content="16:9" Tag="R16x9" />
                <ComboBoxItem Content="4:3" Tag="R4x3" />
                <ComboBoxItem Content="1:1" Tag="R1x1" />
                <ComboBoxItem Content="9:16" Tag="R9x16" />
                <ComboBoxItem Content="Original" Tag="Original" />
            </ComboBox>
        </StackPanel>

        <Viewbox Grid.Row="1"
                 Stretch="Uniform"
                 HorizontalAlignment="Stretch"
                 VerticalAlignment="Stretch">
            <ctrl:CropOverlay x:Name="Overlay" />
        </Viewbox>

        <TextBlock Grid.Row="2" x:Name="CropInfo" Opacity="0.7" />
    </Grid>
</ContentDialog>
